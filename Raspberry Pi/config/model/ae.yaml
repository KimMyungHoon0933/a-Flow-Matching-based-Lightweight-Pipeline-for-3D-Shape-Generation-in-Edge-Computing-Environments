_target_: cod.models.vae.CompactLatentAutoencoder

output_dim: 1
num_latents: 32
embed_dim: 512
query_dim: 32
use_learnable_pos: false

encoder_params:
  num_patches: 512
  num_blocks: 4
  num_layers_per_block: 3

decoder_params:
  output_resolution: 128
  output_patch_size: 8
  num_layers: 12
  num_init_layers: 1
  mlp_ratio: 2
  keep_ratio: 0.25
  num_merged_tokens: 8
  use_conv_refine: false